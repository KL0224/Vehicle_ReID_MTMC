from setuptools import setup, find_packages

setup(
    name="fastreid",
    version="1.0.0",
    description="Fast ReID for Vehicle Re-identification",
    packages=find_packages(),
    python_requires=">=3.7"
)